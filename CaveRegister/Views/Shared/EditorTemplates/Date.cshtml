@model Nullable<DateTime>
@{
	DateTime dt = DateTime.Now;
	if (Model != null)
	{
		dt = (System.DateTime)Model;
	}
	@Html.TextBox("", String.Format("{0:d}", dt.ToString("yyyy-MM-dd")), new { @class = "form-control datecontrol", type = "date" })
}
