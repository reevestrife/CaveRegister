@model Nullable<DateTime>
@{
	DateTime dt = DateTime.Now;
	if (Model != null)
	{
		dt = (System.DateTime)Model;
	}
	@Html.TextBox("", String.Format("{0:HH:mm:ss}", dt), new { @class = "form-control datecontrol", type = "time" })
}
